import { FilterEventsPipe } from './filter-events.pipe';

describe('FilterEventsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEventsPipe();
    expect(pipe).toBeTruthy();
  });
});
